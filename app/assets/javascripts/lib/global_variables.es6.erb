/*
 * decaffeinate suggestions:
 * DS206: Consider reworking classes to avoid initClass
 * Full docs: https://github.com/decaffeinate/decaffeinate/blob/master/docs/suggestions.md
 */
(function() {
  let currentLocationImage = undefined;
  let circleMarkerImage = undefined;
  let showMarkerImage = undefined;
  const Cls = (Refuge.Library.GlobalVariables = class GlobalVariables {
    static initClass() {
      currentLocationImage = "<%= image_path('marker-icon.png') %>";
      circleMarkerImage = "<%= image_path('marker-circle.png') %>";
      showMarkerImage = "<%= image_path('show-icon.png') %>";
    }
  });
  Cls.initClass();
  return Cls;
})();